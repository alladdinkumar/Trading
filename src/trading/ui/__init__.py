"""Phase 15 — Streamlit dashboard.

A read-only Streamlit dashboard over the artefacts the daily jobs produce:
macro snapshots, holdings + GTTs (Kite JSON), today's signals, paper trades,
brief markdowns.

Entry point: `uv run streamlit run src/trading/ui/Home.py`.
"""
